export default function BillingSuccess() {
  return (
    <main className="min-h-screen flex flex-col items-center justify-center px-6 text-center">
      <h1 className="text-4xl text-ink" style={{ fontFamily: "var(--font-fraunces)" }}>
        Welcome to Plate3D Pro
      </h1>
      <p className="mt-3 text-ink-dim max-w-md">
        Your subscription is active. Head back to the app — 3D model generation and multiple
        restaurants are now unlocked.
      </p>
    </main>
  );
}
